# Reference-only sibling project (not part of this app)
online-sermon/

# Bible source XML (cloned at deploy time)
bibles/

# Secrets & local state
.env
*.env
!.env.example
db.sqlite3
*.sqlite3
staticfiles/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Blue/green live upstream — runtime state, rewritten by scripts/deploy-swap.sh.
# Bootstrapped from active-upstream.conf.example on first deploy.
nginx/upstream/active-upstream.conf
